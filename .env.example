# Configurações de ambiente para Base Nest Angular
# Copie este arquivo para .env e ajuste conforme necessário

# ============================================================================
# PORTAS DOS SERVIÇOS
# ============================================================================

# Caddy Reverse Proxy
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
CADDY_ADMIN_PORT=2019

# Backend NestJS (apenas quando EXPOSE_CONTAINER_PORTS=true)
BACKEND_PORT=3000
BACKEND_GRPC_PORT=5001

# Frontend Angular Universal (apenas quando EXPOSE_CONTAINER_PORTS=true)
FRONTEND_PORT=4000

# Databases (apenas quando EXPOSE_CONTAINER_PORTS=true)
POSTGRES_PORT=5432
MONGO_PORT=27017
REDIS_PORT=6379

# ============================================================================
# CONFIGURAÇÕES DE EXPOSIÇÃO
# ============================================================================

# Expor portas dos containers individuais?
# true  = Desenvolvimento/Debug (expõe todas as portas)
# false = Produção real (apenas Caddy exposto)
EXPOSE_CONTAINER_PORTS=true

# ============================================================================
# CONFIGURAÇÕES DE DATABASE
# ============================================================================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=demo

# MongoDB
MONGO_INITDB_DATABASE=demo

# ============================================================================
# CONFIGURAÇÕES DE APLICAÇÃO
# ============================================================================

# Ambiente da aplicação
NODE_ENV=production

# URLs internas (containers se comunicam entre si)
BACKEND_INTERNAL_URL=http://backend:3000
FRONTEND_INTERNAL_URL=http://frontend:4000

# URLs externas (acesso via Caddy)
BACKEND_EXTERNAL_URL=http://api.localhost
FRONTEND_EXTERNAL_URL=http://localhost